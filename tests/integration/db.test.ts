import { describe, it, expect } from "vitest"

describe("database", () => {
    it.todo('adding an image with a new model adds both the image and the model to the database')

    it.todo('adding an image with an ')

    it.todo('partial user searches for prompts return correct results')

    it.todo('searches for exact prompt matches return correct results')

    it.todo('partial prompt search works with both flux and sd generated prompt metadata')

    it.todo('exact prompt search works with both flux and sd generated prompt metadata')

    it.todo('lower case prompt is added to entry in database')
})